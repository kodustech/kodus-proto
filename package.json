{
  "name": "kodus-proto",
  "version": "1.0.0",
  "license": "AGPL-3.0-only",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "dependencies": {},
  "peerDependencies": {
    "@nestjs/microservices": "^11.0.12",
    "rxjs": "^7.8.2"
  },
  "scripts": {
    "prepack": "yarn generate && yarn tsc",
    "types": "tsc",
    "generate": "buf generate",
    "lint": "buf lint",
    "docker:generate": "docker compose -f docker-compose.yml up"
  },
  "files": [
    "dist/**/*",
    "kodus/**/*"
  ],
  "devDependencies": {
    "@bufbuild/buf": "^1.52.1",
    "ts-proto": "^2.7.0",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3"
  }
}